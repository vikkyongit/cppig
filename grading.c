#include<stdio.h>
int main()
    {
        int s;
        printf(" enter the value from 1 to 100\n");
        scanf("%d",&s);
        while(s>=0 && s<=100)
        {
            if (s > 39 && s<50)
                printf(" PASS \n");
        }
        else
            {
    }
